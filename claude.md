# کلود — دفتر مشترک کار

نقطه‌ی ارتباطی پایدار بین محسن و کلود برای این مخزن. کلود بعد از هر کار بخش
«آخرین کارهای انجام‌شده» رو به‌روز می‌کنه؛ محسن هم کار بعدی رو زیر بخش
«کار بعدی» می‌نویسه تا سشن بعدی همون‌جا شروع بشه.

## آخرین کارهای انجام‌شده

**کیبورد دستورات دیگه همیشگی نیست - فقط بعد از /start، تا انتخاب یه گزینه** (۱۴۰۵/۰۵/۲۵ - روی `arabot-dev` دیپلوی شده)

برعکسِ درخواست قبلی («همیشه پایین باشن») - محسن نظرش عوض شد: حالا کیبورد
فقط لحظه‌ی `/start`/`/restart` (کنار پیام خوش‌آمد + پیام «از منوی زیر گزینه
مورد نظرت رو انتخاب کن») نمایش داده می‌شه، و به محض اینکه کاربر هرکدوم از
دستورات رو بزنه (یا حتی تایپ کنه، چون همه‌ی لیبل‌ها الان دقیقاً همون
اسلش‌کامندن) هم پیام هم خود کیبورد جمع می‌شن.

- `command_handlers.py`: `_show_menu` (پیام خوش‌آمد+پرامپت رو با کیبورد
  می‌فرسته، id پیام رو در `user_data["menu_prompt_id"]` نگه می‌داره) و
  `_clear_menu` (پیام رو حذف می‌کنه، و چون تلگرام هیچ راهی برای حذف
  ReplyKeyboardMarkup بدون فرستادن یه پیام واقعی نداره، یه پیام یک‌بارمصرف
  با `ReplyKeyboardRemove()` می‌فرسته و بلافاصله حذفش می‌کنه).
  `_clear_menu` اول هر ۷ کامند (`/report`/`/resource`/`/archive`/`/tags`/
  `/timer`/`/help`/`/stop`) صدا زده می‌شه.
- نکته‌ی جانبی که ارزش دونستن داره: چون دیروز `CMD_LABEL_RESOURCE` (و بقیه)
  رو به متن دقیق اسلش‌کامند تغییر دادی، تلگرام هر پیامی که با `/کلمه` شروع
  بشه رو خودکار کامند تشخیص می‌ده - یعنی زدن دکمه‌های کیبورد دیگه از مسیر
  دیسپچر متنی من (`command_keyboard_message_handler` در message_handlers.py)
  رد نمی‌شه، مستقیم می‌ره به همون `CommandHandler` واقعی. اون دیسپچر و
  `_COMMAND_KEYBOARD_DISPATCH` فعلاً کد مرده‌ان (فقط اگه لیبل‌ها دوباره غیر
  از اسلش‌کامند بشن دوباره فعال می‌شن) - فعلاً دست نزدم بهشون، فقط گفته باشم.
- هنوز کامیت نشده - منتظر تایید محسن.

**رفع خطای docker-compose بعد از تغییرات دستی محسن + ساده‌سازی زیرساخت دیپلوی** (۱۴۰۵/۰۵/۲۵ - روی `arabot-dev` دیپلوی شده)

محسن مستقیم (بیرون از این سشن) چند کامیت زد (`f566d13`، `b0eccc0`،
`7e20bc1`، `0d1726d`) که لیبل‌های کیبورد دستورات رو از فارسی+ایموجی به متن
اسلش‌کامند خام تغییر داد، و `docker-compose.yml` رو از یک تگ مشترک
(`${IMAGE_TAG}`) به سه تگ جدا (`arabot:mig`/`arabot:dev`/`arabot:prod`)
تغییر داد - و بعدش با خطا مواجه شد چون فقط `arabot:dev` واقعاً build
می‌شد.

- ریشه‌ی خطا: `docker compose run arabot-mig` → `pull access denied` چون
  `arabot:mig` هیچ‌وقت لوکال build نشده بود (فقط `arabot:dev`).
- محسن توضیح داد که دیگه با Gitea کار نمی‌کنه، کانتینر مهاجرت جداگونه هم
  دیگه لازم نیست (چون قبلاً بخاطر این بود که دیتابیس روی خود هاست بود، الان
  روی Supabase‌ست و مایگریشن‌ها دستی و مستقیم قبل از پوش اجرا می‌شن - دقیقا
  همون روالی که این سشن از اولش داشتیم)، و شاخه‌ی `development` تنها دیپلوی
  واقعیشه.
- `docker-compose.yml` ساده شد: فقط سرویس `arabot-dev` مونده، روی ایمیج
  ثابت `arabot:latest` (نه `${IMAGE_TAG}`، نه تگ‌های جدا). سرویس‌های
  `arabot-mig`/`arabot-prod` و ولوم `ara_data_prod` حذف شدن.
- `.gitea/workflows/deploy.yml` (پایپ‌لاین CI قدیمی، کاملاً مرده) حذف شد،
  با تایید محسن.
- تایپوی `CMD_LABEL_RESOURCE = "/resources"` (جمع، در حالی که کامند واقعی
  `/resource` مفرده) به `/resource` برگشت - قبلش دکمه‌ی 🧺 کیبورد اصلاً کار
  نمی‌کرد چون تلگرام هر متن `/کلمه` رو خودکار کامند تشخیص می‌ده و از مسیر
  دیسپچر متنی من رد می‌شه، و کامندی به اسم `resources` هم ثبت نشده بود.
- `README.md`: بخش دیپلوی و پشته‌ی فنی با روال جدید (بدون CI، مایگریشن دستی،
  یک سرویس/یک تگ) هماهنگ شد.
- دستور جدید دیپلوی از این به بعد: `docker build -t arabot:latest .` بعد
  `docker compose up -d --force-recreate arabot-dev` (بدون نیاز به
  `IMAGE_TAG=...`).
- کامیت `cee7b27`، روی `origin/development` پوش شده.

- کیبورد ثابت (`main_reply_keyboard`) حالا روی /start، /restart، /help و
  /stop هم ست می‌شه (قبلاً فقط start/restart).
- محدودیت مهم API تلگرام که باید حواسم بهش باشه: `ReplyKeyboardMarkup`
  فقط با `sendMessage` (پیام واقعاً جدید) قابل تنظیمه، نه با `editMessageText`
  - برای همینه که خیلی از پیام‌های ربات (که در واقع edit یه پیام قبلی هستن،
    مثل کارت فعالیت‌ها) اصلاً نمی‌تونن این کیبورد رو حمل کنن. همچنین یک
    پیام فقط یک `reply_markup` می‌تونه داشته باشه - پس پیام‌هایی که خودشون
    inline keyboard دارن (کارت فعالیت، منوی /resource و...) نمی‌تونن همزمان
    کیبورد ثابت رو هم حمل کنن.
  - نکته‌ی خوب: طبق مکانیزم تلگرام، کیبورد ثابت یه بار که ست بشه، سطح چت
    می‌مونه و مستقل از inline keyboard پیام‌های دیگه‌ست - نیازی نیست هر پیام
    دوباره بفرستدش. فقط اگه کاربر topLevel-حالتش رو دستی جمع کنه، با زدن
    آیکون کیبورد کنار جعبه‌ی پیام دوباره باز می‌شه، کاری از ربات ساخته نیست.
  - در نتیجه تنها جایی که واقعاً باگ بود: کاربرهایی که این قابلیت وقتی اضافه
    شد هنوز /start نزده بودن، اصلاً کیبورد رو ندیده بودن. با ست‌کردنش روی
    /help و /stop هم (تنها دو تا کامند دیگه که پیام واقعاً جدید و بدون
    inline keyboard می‌فرستن) این احتمال کمتر شد.
- کامیت `9d5de01`، روی `origin/development` پوش شده.

**دکمه‌ی لغو مشورت هوش مصنوعی + مدیریت دستی تگ‌ها + کیبورد دستورات** (۱۴۰۵/۰۵/۲۵ - روی `arabot-dev` دیپلوی شده، بدون تغییر دیتابیس)

- پیام مشورت هوش مصنوعی روی 📋 توضیحات حالا یه دکمه‌ی «لغو» داره. state ای
  که این پیام توش نمایش داده می‌شه (LLM) اصلاً CallbackQueryHandler نداشت،
  فقط از طریق fallback عمومی `cancel_query_handler` (که خودش امروز صبح
  زودتر `return ACTIVITY` صریح گرفته بود) قابل دسترسیه - همون دکمه‌ی موجود
  رو استفاده کردم، هندلر جدیدی لازم نبود.
- دستور جدید `/tags`: کاملا دستی، دقیقا مثل `tag_conversation.py` نسخه‌ی
  قدیمی جنگو (خونده شد و کد این نسخه هم عیناً همون رفتار رو پیاده می‌کنه):
  صفحه‌ی اصلی (🔍 جستجوی تگ‌های موجود، ➕ افزودن با فقط عنوان)، بعد صفحه‌ی
  ویرایش هر تگ (🔙 بازگشت، 🗑️ حذف فوری بدون تاییدیه - عیناً مثل نسخه‌ی
  قدیمی)؛ تایپ عنوان جدید یه پیام تاییدیه‌ی جداگانه (تایید/لغو) نشون می‌ده.
  3 state جدید: `TAG_HOME`/`TAG_EDIT`/`TAG_CONFIRM`. توابع جدید در
  resource_service.py: `create_tag`، `rename_tag`، `delete_tag` (حذف تگ
  لینک‌هاش به منابع رو هم از طریق backref پاک می‌کنه تا FK نشکنه؛ خود
  منابع دست‌نخورده می‌مونن).
- یه کیبورد ثابت تلگرام (`ReplyKeyboardMarkup`, نه اینلاین) زیر پیام‌ها
  اضافه شد با دکمه‌ی هر دستور به همراه ایموجی (📊 گزارش، 🧺 منابع، 🗃️
  آرشیو، 🏷️ تگ‌ها، ⏲ تایمر، ❓ راهنما، 🛑 توقف) - با /start یا /restart
  تنظیم می‌شه و تا وقتی کاربر عوضش نکنه می‌مونه. زدن دکمه متن لیبلش رو به
  عنوان پیام عادی می‌فرسته؛ یه دیسپچر جدید (`command_keyboard_message_handler`
  در message_handlers.py) این لیبل‌ها رو تشخیص می‌ده و به همون هندلر
  دستور مربوطه می‌فرسته، و برای هر متن دیگه (فعالیت جدید) به مسیر قبلی
  (`create_activity_message_handler`) برمی‌گرده. دستورات با تایپ /command
  هم مثل قبل کار می‌کنن، این فقط یه مسیر جایگزینه.
- کامیت `dee645a`، روی `origin/development` پوش شده.

**آرشیو فعالیت + رفع نبود گزینه‌ی بازگشت در `/resource`** (۱۴۰۵/۰۵/۲۴ - روی `arabot-dev` دیپلوی شده و مایگریشن اجرا شده)

- منوی `/resource` یه دکمه‌ی 🔙 گرفت (کنار 🔍 و ➕) - قبلاً هیچ راهی برای
  انصراف از این صفحه نبود.
- گزینه‌ی جدید 🗃️ داخل منوی ✏️: فعالیت رو آرشیو می‌کنه. ستون جدید
  `Task.archived` (مایگریشن `a4c7e1f92b03`، با `server_default=false` تا
  ردیف‌های قدیمی هم بک‌فیل بشن و NULL نمونن - وگرنه فیلتر `== False` روی
  NULL چیزی برنمی‌گردوند و فعالیت‌های فعال به‌اشتباه از جاب حذف می‌شدن).
  آرشیو کردن فقط `archived=True` می‌کنه؛ به next_date/rrule دست نمی‌زنه،
  فقط `report_service.get_due_tasks` (جاب یادآوری) الان `Task.archived == False`
  رو هم فیلتر می‌کنه، طبق درخواست صریح محسن (فقط جاب، نه گزارش‌های دیگه).
- دستور جدید `/archive`: کاملا دستی، فقط از طریق اینلاین‌کوئری (دکمه‌ی 🔍
  با `switch_inline_query_current_chat='archive:'`) فعالیت‌های آرشیو شده رو
  نشون می‌ده (تایتل = عنوان فعالیت، توضیحات = توضیحات فعالیت). انتخاب
  هرکدوم بلافاصله از آرشیو خارجش می‌کنه و کارتش رو نشون می‌ده - محسن صریحاً
  همین رفتار رو خواست (بدون گام تایید جدا).
  state جدید: `ARCHIVE_BROWSE`. توابع جدید در task_service.py:
  `archive_activity`، `unarchive_activity`، `list_archived_activities`.
- کامیت `3545e7b`، روی `origin/development` پوش شده.

**تکمیل منوی ✏️ و مدیریت کاملا دستی منابع (`/resource`)** (۱۴۰۵/۰۵/۲۳ - روی `arabot-dev` دیپلوی شده)

شش مورد نوشته‌شده در «کار بعدی» انجام شد:

- 🗑️ حذف از روی کارت اصلی برداشته شد و رفت زیرمجموعه‌ی منوی ✏️ (دکمه‌ی
  `editdelete`). باگ جانبی هم رفع شد: `confirm_delete_activity_query_handler`
  و `cancel_query_handler` قبلاً `return` صریح نداشتن (فقط چون همیشه از
  state=ACTIVITY صدا زده می‌شدن مشکلی پیش نمی‌اومد)؛ حالا از EDIT_MENU هم
  صدا زده می‌شن پس هر دو صریحاً `return ACTIVITY` می‌کنن.
- 🔄 ویرایش تکرار فعالیت برگشت به مدل زبانی (به‌جای متن خام RRULE که صبح
  همین امروز دستی شده بود) - چون تشخیص تکرار از روی جمله‌ی فارسی کار زبانیه.
  باگی که قبلاً برای مسیر دستی رفع شده بود (`update_activity_frequency` که
  next_date رو دوباره حساب می‌کنه، چون `update_activity` عمومی این کارو
  نمی‌کنه) حالا به مسیر `edit_activity` مدل هم وصل شد تا دوباره برنگرده.
- 📋 دکمه‌ی توضیحات یه گزینه‌ی 🤖 هم گرفت: تایپ مستقیم هنوز دستیه، ولی
  می‌شه با مدل هم مشورت کرد و در نهایت `edit_activity` توضیحات رو آپدیت کنه.
- `/resource` کاملا دستی شد: صفحه‌ی اصلی (🔍 جستجوی منابع موجود با
  inline-query، ➕ تعریف منبع جدید با فقط عنوان)، بعد صفحه‌ی جزئیات هر منبع
  دقیقا مثل کیبورد `resource_keyboard` نسخه‌ی قدیمی جنگو (🗂️ تگ، 📏 واحد،
  🔄 ضریب تبدیل، 🫙 حداقل موجودی، 🧾 تاریخچه‌ی قیمت، 🗑️ حذف). تگ‌ها هم با
  🔍 (inline-query) یا تایپ مستقیم (ساخت تگ جدید) قابل افزودن/حذفن.
  6 state جدید: `RESOURCE_HOME`/`RESOURCE_DETAIL`/`RESOURCE_FIELD`/
  `RESOURCE_TAG`/`RESOURCE_PRICE`/`RESOURCE_DELETE`.
- حذف منبع = حذف کامل و همیشگی (منبع + قیمت‌ها + ضریب تبدیل + تگ‌ها +
  لینک‌های `TaskResource` + کل تاریخچه‌ی `ResourceLog`) - برخلاف حذف فعالیت
  که سابقه‌دار باشه فقط منجمد می‌شه، اینجا محسن صریحاً تایید کرد که دقیقا
  مثل نسخه‌ی قدیمی، همه‌چی واقعاً پاک بشه.
- مورد «جستجو/افزودن منبع به فعالیت به شکل اینلاین‌کوئری» از قبل (همون
  فلوی 🧺) برآورده بود، کار جدیدی لازم نداشت.
- `create_resource`/`manage_task_resource` طبق دستور قبلی محسن همچنان در
  `tools.json` برای مدل زبانی می‌مونن، فقط دیگه هیچ نقطه‌ی ورودی‌ای بهشون
  وصل نیست.
- کامیت `52ec28c`، روی `origin/development` پوش شده.

**ویرایش فعالیت و مدیریت منابع به‌صورت دستی (بدون مدل زبانی)** (۱۴۰۵/۰۵/۲۳ - روی `arabot-dev` دیپلوی شده)

دکمه‌های ✏️ و 🧺 روی کارت فعالیت دیگه یک گفتگوی مدل زبانی رو باز نمی‌کنن؛
کاملاً با دکمه و متن ساده کار می‌کنن، شبیه رفتار همون بات قدیمی جنگو
(شاخه‌ی `legacy-bot-version`, کیبورد `task_edit_keyboard`) ولی هماهنگ‌شده
با معماری فعلی که هر فعالیت تکرارشونده فقط یک ردیف در جدول `Task` داره:

- ✏️ یک منوی ویرایش باز می‌کنه: 🏷️ عنوان، 📋 توضیحات، 📆 تاریخ (با یک تقویم
  جلالی، `src/app/bot/shared/jalali_calendar.py`)، 🔄 تکرار (متن خام RRULE
  که قبل از ذخیره اعتبارسنجی می‌شه)، 🟠🔵 کپی (کپی از فعالیت + منابع
  وصل‌شده بهش). دو state جدید: `EDIT_MENU`/`EDIT_FIELD`.
- 🧺 یک منوی مدیریت منابع باز می‌کنه: منابع فعلاً وصل‌شده با دکمه‌ی ✖️
  نمایش داده می‌شن؛ دکمه‌ی 🔍 از قابلیت inline-query تلگرام
  (`switch_inline_query_current_chat`) برای انتخاب یک منبع موجود استفاده
  می‌کنه و بعد فقط مقدارش رو به‌صورت متن می‌پرسه. دو state جدید:
  `RESOURCE_MENU`/`RESOURCE_QTY`.
- توابع `edit_activity`، `create_resource`، `manage_task_resource` هنوز هم
  برای مدل زبانی در `tools.json`/`instructions.md` فعالن - فقط همین دو
  دکمه دیگه ازشون استفاده نمی‌کنن. `/report` و `/resource` (تعریف منبع
  جدید) همچنان با مدل زبانی کار می‌کنن.
- هیچ تغییری در ساختار دیتابیس نداشت. توابع سرویس جدید: `copy_activity`،
  `update_activity_frequency`، `describe_rrule`، `get_activity_datetime`
  (در task_service.py)؛ `list_task_resource_links`، `link_task_resource_by_id`،
  `unlink_task_resource_by_id` (در resource_service.py).
- همراه با ویژگی زیر (سابقه‌ی مصرف منابع) در کامیت `9bc8e51` ثبت و روی
  `origin/development` پوش شده.

**قابلیت ردیابی مصرف منابع/انبار** (۱۴۰۵/۰۵/۲۲ - روی `arabot-dev` دیپلوی شده)

از ماژول resource همون بات جنگوی قدیمی پورت شده و با معماری rrule فعلی
هماهنگ شده: `Resource`/`Tag`/`ResourceParity`/`ResourcePrice` (کامل مثل
نسخه‌ی قدیمی)، `TaskResource` (لینک الگو: فعالیت به منبع با یک مقدار
علامت‌دار ثابت که فقط یک‌بار تنظیم می‌شه)، `ResourceLog` (یک ردیف تاریخ‌دار
که هر بار با ✔️ تایید فعالیت نوشته می‌شه، نه با ✖️ رد کردنش). حذف فعالیتی
که سابقه‌ی مصرف منبع داره، دیگه واقعاً حذف نمی‌شه بلکه فقط تکرارهای بعدیش
متوقف می‌شه (`skip_future_activities`) تا سابقه‌ش از بین نره. مایگریشن‌های
`ba1771db5274` (ساخت جدول‌های اولیه) و `0ec19298f798` (رفع باگ سرریز عدد
۳۲‌بیتی در `resources.user_id` که در محیط پروداکشن پیدا شد).

## کار بعدی

(محسن: کار بعدی رو اینجا بنویس)
