name: Deploy arabot

on:
  push:
    branches:
      - main
      - development

jobs:
  deploy:
    runs-on: ara

    env:
      BRANCH: ${{ gitea.ref_name }}

      DATABASE_URL_DEV: ${{ secrets.DATABASE_URL_DEV }}
      DATABASE_URL_PROD: ${{ secrets.DATABASE_URL_PROD }}

      BOT_TOKEN_DEV: ${{ secrets.BOT_TOKEN_DEV }}
      BOT_TOKEN_PROD: ${{ secrets.BOT_TOKEN_PROD }}

      AI_TOKEN: ${{ secrets.AI_TOKEN }}

      BOT_URL: ${{ vars.BOT_URL }}
      AI_URL: ${{ vars.AI_URL }}

      AI_MODEL_DEV: ${{ vars.AI_MODEL_DEV }}
      AI_MODEL_PROD: ${{ vars.AI_MODEL_PROD }}

      TIMEZONE: ${{ vars.TIMEZONE }}
      CALENDAR: ${{ vars.CALENDAR }}

      BOT_PERSISTENCE_DEV: ${{ vars.BOT_PERSISTENCE_PATH_DEV }}
      BOT_PERSISTENCE_PROD: ${{ vars.BOT_PERSISTENCE_PATH_PROD }}

    steps:
      - name: Checkout repository
        shell: bash
        run: |
          set -e

          REPO_URL="ssh://git@87.248.139.251:2222/ARACOM/ara.git"

          rm -rf repo
          git clone --depth 1 --branch "$BRANCH" "$REPO_URL" repo

      - name: Deploy
        shell: bash
        run: |
          set -e
          cd repo

          if [ "$BRANCH" = "development" ]; then
            export DATABASE_URL="$DATABASE_URL_DEV"
            export BOT_TOKEN="$BOT_TOKEN_DEV"
            export AI_MODEL="$AI_MODEL_DEV"
            export BOT_PERSISTENCE_URL="$BOT_PERSISTENCE_DEV"
            SERVICE="arabot-dev"

          elif [ "$BRANCH" = "main" ]; then
            export DATABASE_URL="$DATABASE_URL_PROD"
            export BOT_TOKEN="$BOT_TOKEN_PROD"
            export AI_MODEL="$AI_MODEL_PROD"
            export BOT_PERSISTENCE_URL="$BOT_PERSISTENCE_PROD"
            SERVICE="arabot-prod"

          else
            echo "Unsupported branch: $BRANCH"
            exit 1
          fi

          echo "Deploying $SERVICE"

          docker compose run --rm arabot-mig
          docker compose up -d --build $SERVICE

          git clean -fdx || true
